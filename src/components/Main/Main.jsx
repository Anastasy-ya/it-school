import './Main.css';

function Main() {

  
  return (
    <main className="content">
      <section className="">
        
      </section>

    </main>
  );
}

export default Main;

// background-color: ${({ theme }) => theme.colors.mainBackground};